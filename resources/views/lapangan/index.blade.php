@extends('layouts.app') @section('title','Lapangan') @section('content')<div class="d-flex justify-content-between mb-3"><h2>Data Lapangan</h2><a class="btn btn-primary" href="{{ route('lapangan.create') }}">Tambah Lapangan</a></div><form class="mb-3"><div class="input-group"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Cari lapangan..."><button class="btn btn-outline-secondary">Cari</button></div></form><div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Kode</th><th>Nama</th><th>Jenis</th><th>Harga/Jam</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($data as $x)<tr><td>{{ $x->kode_lapangan }}</td><td>{{ $x->nama_lapangan }}</td><td>{{ ucfirst($x->jenis) }}</td><td>Rp{{ number_format($x->harga_per_jam,0,',','.') }}</td><td>{{ ucfirst($x->status) }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('lapangan.edit',$x) }}">Edit</a><form class="d-inline" method="post" action="{{ route('lapangan.destroy',$x) }}" onsubmit="return confirm('Hapus data?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $data->links() }}</div>@endsection