@extends('layouts.app') @section('content')<h2>{{ $item->exists?'Edit':'Tambah' }} Lapangan</h2><div class="card shadow-sm"><div class="card-body"><form method="post" action="{{ $item->exists?route('lapangan.update',$item):route('lapangan.store') }}">@csrf @if($item->exists) @method('PUT') @endif<div class="row"><div class="col-md-6 mb-3"><label>Kode</label><input name="kode_lapangan" value="{{ old('kode_lapangan',$item->kode_lapangan) }}" class="form-control" required></div><div class="col-md-6 mb-3"><label>Nama</label><input name="nama_lapangan" value="{{ old('nama_lapangan',$item->nama_lapangan) }}" class="form-control" required></div><div class="col-md-4 mb-3"><label>Jenis</label><select name="jenis" class="form-select"><option value="futsal" @selected(old('jenis',$item->jenis)==='futsal')>Futsal</option><option value="badminton" @selected(old('jenis',$item->jenis)==='badminton')>Badminton</option></select></div><div class="col-md-4 mb-3"><label>Harga/Jam</label><input type="number" name="harga_per_jam" value="{{ old('harga_per_jam',$item->harga_per_jam) }}" class="form-control" required></div><div class="col-md-4 mb-3"><label>Status</label><select name="status" class="form-select">@foreach(['tersedia','perawatan','nonaktif'] as $s)<option @selected(old('status',$item->status?:'tersedia')===$s)>{{ $s }}</option>@endforeach</select></div></div><div class="mb-3"><label>Keterangan</label><textarea name="keterangan" class="form-control">{{ old('keterangan',$item->keterangan) }}</textarea></div>@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<a href="{{ route('lapangan.index') }}" class="btn btn-secondary">Kembali</a><button class="btn btn-primary">Simpan</button></form></div></div>@endsection