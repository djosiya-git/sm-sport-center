@extends('layouts.app') @section('content')<div class="d-flex justify-content-between mb-3"><h2>Data Pelanggan</h2><a class="btn btn-primary" href="{{ route('pelanggan.create') }}">Tambah Pelanggan</a></div><form class="mb-3"><div class="input-group"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Cari pelanggan..."><button class="btn btn-outline-secondary">Cari</button></div></form><div class="card shadow-sm"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Kode</th><th>Nama</th><th>Email</th><th>Telepon</th><th>Aksi</th></tr></thead><tbody>@foreach($data as $x)<tr><td>{{ $x->kode_pelanggan }}</td><td>{{ $x->nama }}</td><td>{{ $x->user?->email }}</td><td>{{ $x->no_telepon }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('pelanggan.edit',$x) }}">Edit</a><form class="d-inline" method="post" action="{{ route('pelanggan.destroy',$x) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $data->links() }}</div>@endsection