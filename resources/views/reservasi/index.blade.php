@extends('layouts.app') @section('content')<div class="d-flex justify-content-between mb-3"><h2>Data Reservasi</h2><a class="btn btn-primary" href="{{ route('reservasi.create') }}">Tambah Reservasi</a></div><form class="row g-2 mb-3"><div class="col-md-6"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Cari kode, pelanggan, lapangan..."></div><div class="col-md-3"><input type="date" name="tanggal" value="{{ request('tanggal') }}" class="form-control"></div><div class="col-md-3"><button class="btn btn-outline-secondary w-100">Cari</button></div></form><div class="card shadow-sm"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Kode</th><th>Pelanggan</th><th>Lapangan</th><th>Tanggal</th><th>Jam</th><th>Total</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($data as $x)<tr><td>{{ $x->kode_reservasi }}</td><td>{{ $x->pelanggan->nama }}</td><td>{{ $x->lapangan->nama_lapangan }}</td><td>{{ $x->tanggal->format('d/m/Y') }}</td><td>{{ substr($x->jam_mulai,0,5) }}-{{ substr($x->jam_selesai,0,5) }}</td><td>Rp{{ number_format($x->total_harga,0,',','.') }}</td><td>{{ $x->status_reservasi }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('reservasi.edit',$x) }}">Edit</a><form class="d-inline" method="post" action="{{ route('reservasi.destroy',$x) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form></td></tr>@empty<tr><td colspan="8" class="text-center">Belum ada reservasi.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $data->links() }}</div>@endsection