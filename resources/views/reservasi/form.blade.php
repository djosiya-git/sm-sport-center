@extends('layouts.app') @section('content')<h2>{{ $item->exists?'Edit':'Tambah' }} Reservasi</h2><div class="card shadow-sm"><div class="card-body"><form method="post" action="{{ $item->exists?route('reservasi.update',$item):route('reservasi.store') }}">@csrf @if($item->exists) @method('PUT') @endif @if(auth()->user()->role==='admin')<div class="mb-3"><label>Pelanggan</label><select name="pelanggan_id" class="form-select" required><option value="">Pilih pelanggan</option>@foreach($pelanggans as $p)<option value="{{ $p->id }}" @selected(old('pelanggan_id',$item->pelanggan_id)==$p->id)>{{ $p->kode_pelanggan }} - {{ $p->nama }}</option>@endforeach</select></div>@endif<div class="mb-3"><label>Lapangan</label><select name="lapangan_id" class="form-select" required><option value="">Pilih lapangan</option>@foreach($lapangans as $l)<option value="{{ $l->id }}" @selected(old('lapangan_id',$item->lapangan_id)==$l->id)>{{ $l->nama_lapangan }} - Rp{{ number_format($l->harga_per_jam,0,',','.') }}/jam</option>@endforeach</select></div><div class="row"><div class="col-md-4 mb-3"><label>Tanggal</label><input type="date" name="tanggal" value="{{ old('tanggal',$item->tanggal?->format('Y-m-d')) }}" min="{{ now()->format('Y-m-d') }}" class="form-control" required></div><div class="col-md-4 mb-3"><label>Jam Mulai</label><input type="time" name="jam_mulai" value="{{ old('jam_mulai',substr($item->jam_mulai??'',0,5)) }}" class="form-control" required></div><div class="col-md-4 mb-3"><label>Jam Selesai</label><input type="time" name="jam_selesai" value="{{ old('jam_selesai',substr($item->jam_selesai??'',0,5)) }}" class="form-control" required></div></div><div class="mb-3"><label>Catatan</label><textarea name="catatan" class="form-control">{{ old('catatan',$item->catatan) }}</textarea></div>@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif<a href="{{ route('reservasi.index') }}" class="btn btn-secondary">Kembali</a><button class="btn btn-primary">Simpan</button></form></div></div>@endsection